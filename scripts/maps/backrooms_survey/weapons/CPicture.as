class CPicture
{
    Vector position;
    Vector angles;
    EHandle handle;

    CPicture( Vector _position, Vector _angles, EHandle _handle )
    {
        position = _position;
        angles = _angles;
        handle = _handle;
    }
}
